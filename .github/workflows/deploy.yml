name: Deploy to cPanel (FTP)

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Checkout code
        uses: actions/checkout@v4

      - name: 🚀 Sync via Plain FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASS }}
          port: 21
          passive: false
          # We use 'ftp' and 'secure: false' because 'ftps' usually 
          # causes the timeout/mode error on strict firewalls.
          protocol: ftp 
          secure: false
          local-dir: ./
          server-dir: ./ 
          exclude: |
            **/node_modules/**
            **/vendor/**
            **/.git*
            **/.github/**
            **/__pycache__/**
            **/.env
